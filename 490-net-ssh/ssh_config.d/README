To create a keyword list (KEYWORDS_USED.txt.<version>), execute:

    ssh -G | sort -u > KEYWORDS_USED.txt.<version>


Useful when comparing against older versions and makes easier
to track new/old/changed keyword options.

    diff -y KEYWORDS_USED.txt.8.8 KEYWORDS_USED.txt.9.2

