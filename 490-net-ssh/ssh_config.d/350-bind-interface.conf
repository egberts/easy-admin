#
# File: 350-bind-interface.conf
# Path: /etc/ssh/ssh_config.d
# Title: Bind to a Specific Netdev Interface
# Description:
#
#   BindInterface use the address of the specified 
#   interface on the local machine as the source 
#   address of the connection.
#
#   'BindInterface' binds to the address of 
#   bind_interface before attempting to connect to 
#   the destination host.
#
#   This is only useful on systems with more than 
#   one address.
#
# CLI option: -B
# CLI option: -oBindInterface=<netdev-name>
# Process context: client
# Network stage: Connection establishment
# Message state: n/a
# Variable: options.bind_interface
# Source (read): ssh_create_socket()/ssh_connect_direct()/ssh_connect()/main()
#
#BindInterface eth0

