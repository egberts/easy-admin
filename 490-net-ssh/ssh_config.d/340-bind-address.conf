#
# File: 340-bind-address.conf
# Path: /etc/ssh/ssh_config.d
# Title: 
# Description:
#
#   BindAddress binds the specified address on the local 
#   machine as the source address of the connection.  
#
#   Only useful on systems with more than one address.
#
# CLI option: -b <ip_address>
# CLI option: -oBindAddress=<ip_address>
# Process context: client
# Network stage: Connection establishment
# Message state: n/a
# Variable: options.bind_address
# Source (read): ssh_create_socket()/ssh_connect_direct()/ssh_connect()/main()
#
#BindAddress 192.168.1.1

