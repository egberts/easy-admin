#
# File: 680-hostkey-alias.conf
# Path: /etc/ssh/ssh_config.d
# Title: Alias to Hostkey
# Description:
#
#   'HostKeyAlias' specifies an alias that should be
#   used instead of the real host name when looking
#   up or saving the host key in the host key
#   database files and when validating
#   host certificates.
#
#   This option is useful for tunneling SSH
#   connections or for multiple servers running on a
#   single host.
#
# CLI option: -oHostKeyAlias=<string>
# Process context: client
# Network Stage: Key Exchange (KEX) negotiation
# Message state: n/a
# Variable: options.host_key_alias
# Source (read): check_host_key()/verify_host_key()/verify_host_key_callback/kex_verify_host_key()/input_kex_gen_reply()/ssh_dispatch_run()/ssh_dispatch_run_fatal()/ssh_userauth2()/ssh_login()/main()
# Source (read): userauth_passwd()/userauth()/ssh_dispatch_run()/ssh_dispatch_run_fatal()/ssh_userauth2()/ssh_login()/main()
#
#HostKeyAlias ssh  # no default

