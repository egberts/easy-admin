#
# File: 767-control-path.conf
# Path: /etc/ssh/ssh_config or included ssh_config.d/*.conf
# Title: Control socket path for connection multiplexing
# Description:
#
#   'ControlPath' specifies the file specification path for the
#   control master.
#
#   Specifies the filesystem path used for the control socket when
#   connection multiplexing is enabled via ControlMaster. The client
#   will listen on this socket and other client instances can reuse
#   the existing connection rather than re-establishing a new one.
#   Path may include tokens like %h (host), %r (remote user), %p (port). 
#   Valid values: any usable filesystem path template that resolves at runtime.
#
# CLI option: -o ControlPath=<path>
# Introduced: 3.9
# Obsoleted:
# Process context: utility (ssh client)
# Network stage: Connection Initialization
# Dispatch: n/a
# Channel type: n/a
# Channel name: none
# Control type: n/a
# Mode: n/a
# Variable: options.control_path
# Source (read): main()
# Source (read): client_repledge()/client_loop()/ssh_session2()/main()
# Source (read): client_repledge()/client_input_hostkeys()/client_input_global_request()/client_init_dispatch()/client_loop()/ssh_session2()/main()
# Source (read): client_repledge()/client_global_hostkeys_prove_confirm()/client_input_hostkeys()/client_input_global_request()/client_init_dispatch()/client_loop()/ssh_session2()/main()
# Source (read): client_repledge()/client_session2_setup()/main()
# Source (read): client_loop()/ssh_session2()/main()
# Source (read): client_stop_mux()
# Source (read): control_persist_detach()/fork_postauth()/ssh_session2()/main()
# Source (read): control_persist_detach()/fork_postauth()/forwarding_success()/ssh_confirm_remote_forward()/ssh_init_forwarding()/main()
# Source (read): control_persist_detach()/fork_postauth()/forwarding_success()/ssh_tun_confirm()/ssh_init_forwarding()/main()
# Source (read): ssh_proxy_fdpass_connect()
# Source (read): ssh_proxy_connection()
# Source (read): mux_master_process_stop_listening()
# Source (read): muxserver_listen()
# Source (read): cleanup_exit()/*
# Source (read): fill_default_options()/main()
# Source (read): dump_client_config()/main()
# Source (write): main()
# Source (write): initialize_options()/main()
# Source (write): process_config_line_depth()/main()
# Source (write): process_config_line_depth()/main()
#
ControlPath no

