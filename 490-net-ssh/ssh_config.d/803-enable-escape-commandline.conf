#
# File: 803-enable-escape-commandline.conf
# Path: /etc/ssh/ssh_config or included ssh_config.d/*.conf
# Title: SSH escape command line availability
# Description:
#
#   Controls whether the client‑side ~C escape command menu is enabled
#   for interactive sessions.
#
#   By default this menu is disabled, so typing ~C during a session
#   will not prompt for additional commands.
#
#   Valid values: yes | no.
#
#   Default: no in newer releases.

# CLI option: -o EnableEscapeCommandline=yes|no
# Introduced: 9.2
# Obsoleted:
# Process context: utility (ssh client)
# Network stage: Session setup
# Dispatch: n/a
# Channel type: n/a
# Channel name: none
# Control type: session
# Mode: n/a
# Variable: options.enable_escape_commandline
# Source (read): client_repledge()/client_loop()/ssh_session2()/main()
# Source (read): client_repledge()/client_input_hostkeys()/client_input_global_request()/client_init_dispatch()/client_loop()/ssh_session2()/main()
# Source (read): client_repledge()/client_global_hostkeys_prove_confirm()/client_input_hostkeys()/client_input_global_request()/client_init_dispatch()/client_loop()/ssh_session2()/main()
# Source (read): client_repledge()/client_session2_setup()/main()
# Source (read): process_escapes()/client_simple_escape_filter()/client_loop()/ssh_session2()/main()
# Source (read): print_escape_help()/process_escapes()/client_simple_escape_filter()/client_loop()/ssh_session2()/main()
#
EscapeCommandLine yes

