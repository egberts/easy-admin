#
# File: 210-canonicalize-max-dots.conf
# Path: /etc/ssh/ssh_config.d
# Title: Maximum dots that can be canonicalized
# Description:
#
#   'CanonicalizeMaxDots' specifies the maximum number
#   of dot characters in a hostname before
#   canonicalization is disabled.
#
#   The default '1' allows a single dot
#   (i.e. hostname.subdomain).
#
#       CanonicalizeMaxDots 1  # default
#
# CLI option: -o CanonicalizeMaxDots=<integer>
# Introduced: 6.5
# Obsoleted:
# Process context: client
# Network stage: Initialization
# Message state: n/a
# Variable: options.canonicalize_max_dots
# Source (read): resolve_canonicalize()/main()
# Source (read): dump_client_config()/main()
# Source (write): process_config_line_depth()/process_config_line()/main()
# Source (write): initialize_options()/main()
#
CanonicalizeMaxDots 1

