#
# File: 011-include.conf
# Path: /etc/ssh/ssh_config.d
# Title: Include statement
# Description:
#
#   'Include' includes the specified configuration
#   file(s).
#
#   Multiple pathname may be specified and each
#   pathname may contain glob(7) wildcards and, for
#   user configurations, shell-like ‘~’ references to
#   user home directories.
#
#   Files without absolute paths are assumed to be
#   in ~/.ssh if included in a user configuration
#   file or /etc/ssh if included from the system
#   configuration file.
#
#   'Include' directive may appear inside a 'Match'
#   or 'Host' block to perform conditional inclusion.
#
# CLI option: n/a (parser token)
# Introduced: 7.3
# Obsoleted:
# Process context: client
# Network stage: Initialization
# Message state: n/a
# Variable: n/a (internal control processing)
# Source (read): process_config_line_depth()/main()/ssh.c

#Include /etc/ssh/ssh_config_local

